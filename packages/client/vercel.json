{
    "builds": [
        {
            "src": "packages/client/**",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "packages/client/dist/vercelClient"
            }
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}