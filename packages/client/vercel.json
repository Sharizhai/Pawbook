{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],

    "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "buildCommand": "pnpm --filter pawbook-client run build"
  }
}